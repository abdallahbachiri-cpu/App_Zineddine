{% set _locale = locale|default('en') %}

{{ 'email.hello_name'|trans({'%name%': user.firstName}, 'messages', _locale) }},

{{ 'password_reset.requested'|trans({}, 'messages', _locale) }}

{{ 'password_reset.reset_button'|trans({}, 'messages', _locale) }}: {{ resetLink }}

{{ 'password_reset.ignore_if_not_requested'|trans({}, 'messages', _locale) }}