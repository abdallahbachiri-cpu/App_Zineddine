{% set _locale = locale|default('en') %}

<p>{{ 'email.hello_name'|trans({'%name%': user.firstName}, 'messages', _locale) }},</p>
<p>{{ 'password_reset.requested'|trans({}, 'messages', _locale) }}</p>

<p>
    <a href="{{ resetLink }}" style="background:#007bff;color:white;padding:10px 20px;text-decoration:none;border-radius:5px;">
        {{ 'password_reset.reset_button'|trans({}, 'messages', _locale) }}
    </a>
</p>

<p>{{ 'password_reset.ignore_if_not_requested'|trans({}, 'messages', _locale) }}</p>